<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomerButtonHallo">
        <item name="android:background">@drawable/button_focused_standard</item>
    </style>

    <style name="BottomNavigationCustomStyle">
        <item name="android:elevation">4dp</item>
        <item name="android:shadowColor">@color/colorPrimary</item>
        <item name="android:background">@color/colorOffWhite</item>
    </style>

</resources>
